import { HashFormat } from './interface/HashFormat.js';
export class todo {
    constructor(container) {
        this.container = container;
        render(task, HashFormat, heading, String);
    }
}
